# TODO

- **Embedded-game-plan profile fixture** — couldn't reproduce in the game so far. Add a real fixture to `tests/data/` when one is available, then re-point `test_embedded_game_plan_count_raises_unsupported` and `test_embedded_g95_after_i95_raises_unsupported` to use it instead of synthetic bytes.
- **ruff / pyright cleanup pass** — no findings outstanding (ruff and pyright both clean).
