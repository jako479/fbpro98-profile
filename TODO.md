# TODO

- **Write/update API** — pending design of mutation operations. `schema.py` and
  `model.py` are already written with a writer in mind.
- **ruff / pyright cleanup pass** — no findings outstanding (ruff and pyright both clean).
